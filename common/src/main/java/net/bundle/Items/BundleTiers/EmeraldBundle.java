package net.bundle.Items.BundleTiers;

/**
 * Emerald bundle - 64 slots (full stack equivalent).
 * Obtained via trade from max-level Leatherworker (32 emeralds + Diamond Bundle).
 */
public class EmeraldBundle extends TieredBundleItem {

    public EmeraldBundle(Properties properties) {
        super(properties, 63);
    }
}
